package com.example.cecs453finalproject.interfaces;

// DrawerLocker interface provides functionality to lock the navigation drawer before login.
public interface DrawerLocker {
    public void setDrawerLocked(boolean shouldLock);
}
